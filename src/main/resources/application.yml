spring:
  application:
    name: MovieRepository

  datasource:
    url: jdbc:postgresql://localhost:5432/movies_repository
    username: postgres
    password: 128875
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true

logging:
  level:
    root: warn
    com.mlastovsky.controller: info